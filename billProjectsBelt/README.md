This is the only directory that has a manually defined default.nix and is used to define the machinery to automate the behaviour of default.nix for other directories. `withDebug.nix` defines the `withDebug` function and is the only function-file in all of my nix projects that is not of the form `withDebug rec { ... }`.
